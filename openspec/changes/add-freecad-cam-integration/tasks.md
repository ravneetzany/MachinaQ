## 1. Parser prerequisite: cylinder/cone axis position

- [x] 1.1 In `src/parser.py`, extend `_extract_primitives()`'s `CYLINDRICAL_SURFACE`/`CONICAL_SURFACE` handling to resolve the axis position via the existing `_resolve_placement()` (reusing what `PLANE` already does), and update `GeometryPrimitives.cylinders`/`.cones`' stored tuples to carry `(point, direction)` alongside their existing fields; verify with a unit test parsing a STEP file containing a cylindrical face and asserting a non-default resolved point/direction is present
- [x] 1.2 In `src/pipeline.py`'s `_extract_primitives()`, store the resolved axis position into each cylindrical/conical `SurfacePrimitive.details` via `geometry.axis_to_details()` (the same convention `.scad`/`.py` ingestion and `operation_classifier` already expect); verify with a unit test asserting a STEP-derived cylindrical `SurfacePrimitive.details` now round-trips through `geometry.axis_from_details()` to a valid `Axis`
- [x] 1.3 Run the full existing test suite and confirm no regressions from this parser/pipeline change (in particular `tests/test_parser_geometry.py`, `tests/test_pipeline.py`, `tests/test_operation_classifier*.py`); verify with `pytest tests/ --ignore=tests/test_parser.py` passing

## 2. FreeCAD addon scaffold

- [x] 2.1 Create `freecad_addon/MachinaQCAM/` with `InitGui.py`, `package.xml` (FreeCAD addon metadata), and an icon; `InitGui.py` defines and registers a `Gui.Command` subclass named `MachinaQ_ClassifyFeature` via `Gui.addCommand`
  - **Verification caveat:** the stated verification (load in FreeCAD, confirm registration) could not be performed — this sandbox's `freecadcmd` exposes a stubbed `FreeCADGui` with only 4 attributes (`embedToWindow`, `exec_loop`, `setupWithoutGUI`, `showMainWindow`; confirmed via direct probe), no `addCommand`/`getWorkbench`/`Selection`/`Control`; and running the full GUI (`freecad --console` + `Gui.showMainWindow()`) hangs waiting for a real display (`$DISPLAY=:0` is set but no `Xvfb`/`xvfb-run` is installed, and the probe had to be killed after timing out). What *was* verified: all addon files pass `ast.parse` (syntax-valid); `commands.py`/`InitGui.py`/`task_panel.py`'s API usage (`Gui.addCommand`, `Gui.listCommands`, `self.appendToolbar`, `from PySide import QtGui, QtCore`) matches exactly what this environment's real, installed `/usr/lib/freecad/Mod/CAM/*.py` source uses (grepped and cross-checked, not assumed from memory).
- [x] 2.2 ~~Hook the CAM workbench's activation to call `Gui.activeWorkbench().appendToolbar(...)`~~ **Superseded — register MachinaQ as its own standalone workbench instead.**
  - **Full real-world debugging trail (found via user report after `/opsx:apply`, across several live-testing rounds):** (1) wrapping `CAMWorkbench.Initialize` loaded with no errors but the button never appeared, because `Initialize()` had already run before the patch installed (CAM was the active/default workbench at startup) — patching a method that will never run again fails silently. (2) Replaced with `Gui.getMainWindow().workbenchActivated`, a signal connected via a polling `QTimer` — the same mechanism this FreeCAD 1.1.3 install's own bundled `Tux/PersistentToolbarsGui.py` addon uses — but command registration itself then failed, traced to `Gui.getMainWindow()` being cached once, too early (returns `None`/raises at that point in startup), *before* `commands.register()` in the file, silently aborting the whole script. (3) Fixed that ordering and anchored the timer's Python reference to the `FreeCAD` module (ruling out premature GC of an unparented `QTimer`) — command registration became reliable, but the toolbar-append step still silently never auto-fired, even though every piece (`Gui.activeWorkbench()`, `.appendToolbar(...)`, `Gui.getMainWindow()`) worked when called directly in the console. (4) Rewrote to call the toolbar-append immediately/synchronously plus three independent fallback layers — still intermittently failed in real testing (toggled visible/not-visible across restarts with no further diagnosable signal). **Per the user's explicit choice at that point**, switched to `Gui.addWorkbench(MachinaQWorkbench())` — the same mechanism CAM/PartDesign/every built-in workbench uses to register *itself* (verified: a bare `Workbench` base class is a FreeCAD-injected `InitGui.py` global, not imported, matching `/usr/lib/freecad/Mod/CAM/InitGui.py` and `.../PartDesign/InitGui.py` exactly) — eliminating the "inject into another workbench's internals" timing dependency entirely. See design.md decision 2's full correction history and "Final decision" for details.
  - **Actual root cause found (via FreeCAD's own startup Report View log, direct evidence):** `InitGui.py` was never being executed at all — FreeCAD's log showed it looking for a doubly-nested path (`MachinaQCAM/MachinaQCAM/InitGui.py`) instead of the flat path where the file actually lives. Root cause: `package.xml` was missing a `<subdirectory>./</subdirectory>` element inside its `<workbench>` block — confirmed by comparing against the working `Fasteners` addon's `package.xml`, which has exactly that element. Every prior fix in this task's history was real and individually correct, but none of them could have mattered, since the file was never being read in the first place. Fixed by adding `<subdirectory>./</subdirectory>` to `package.xml` (and correcting its stale `<classname>CAMWorkbench</classname>` to `MachinaQWorkbench`).
  - **User-confirmed working end-to-end**, live: FreeCAD's Report View showed `MachinaQCAM: MachinaQ workbench registered.` and `Initializing .../MachinaQCAM/./... done`; clicking the button in the MachinaQ workbench exported a real STEP file (`Step File Name : ... Write Done`), called the MachinaQ API, and the classification panel appeared with results. Seven corrections across five root causes were needed to get here (CAM-toolbar injection abandoned after four; then, for the standalone workbench: a missing `<subdirectory>` element in `package.xml` meant `InitGui.py` was never even found; then `__file__` not being set by FreeCAD's `exec()`; then a split globals/locals `exec()` scoping bug that took three iterations, verified via direct simulation, to fully resolve) — full history in design.md decision 2.

## 3. HTTP client

- [x] 3.1 Create `freecad_addon/MachinaQCAM/http_client.py` with a `classify(step_path, api_url) -> dict` function that POSTs to `<api_url>/analyze` (using `requests` if importable in FreeCAD's Python, else `urllib.request`), with a configurable timeout, raising a clear exception distinguishing "unreachable" from "error response" per the spec's two error scenarios; verify with a unit test (runnable under the project's own `.venv`, since this module has no FreeCAD-specific imports) mocking both an unreachable host and a non-2xx response, asserting each raises a distinguishable exception
- [x] 3.2 Add a FreeCAD preference (or a simple config default) for the MachinaQ API base URL, defaulting to `http://127.0.0.1:8000`; verify by confirming `http_client.classify()` is called with the configured URL, not a hardcoded one

## 4. Command behavior: export, classify, display

- [x] 4.1 Implement the command's `Activated()`: determine the export target (selected Body, else whole active document) per the spec's two scenarios, export it to a temporary STEP file via FreeCAD's `Part.export`
  - **Verification caveat:** same headless-environment limitation as 2.1/2.2 (no live FreeCAD selection/document to run against). Implemented in `commands.py`'s `_export_target()`/`Activated()` per the spec's two scenarios exactly; `Part.makeCompound`/`Shape.exportStep` usage follows FreeCAD's documented API, not verified by an actual export in this session.
- [x] 4.2 Call `http_client.classify()` with the exported path and handle both error scenarios from the spec (unreachable API, error response) by showing a FreeCAD message box / report-view error rather than raising an uncaught exception
  - **Verification caveat:** the underlying error-distinguishing logic (`MachinaQUnreachableError` vs `MachinaQErrorResponse`) is the same `http_client.py` code verified by real, passing unit tests (`tests/test_freecad_http_client.py`) under `.venv`; only the FreeCAD-side `FreeCAD.Console.PrintError(...)` call sites in `commands.py` are unverified live (same headless limitation).
- [x] 4.3 Build the read-only task-panel UI (PySide/QtWidgets, per design.md decision 5) showing the part-level `operations_summary` and the per-feature type/operation/rationale table
  - **Verification caveat:** `task_panel.py`'s Qt widget usage was cross-checked against this environment's real installed CAM addon source (`Path/Main/Gui/*.py`'s `from PySide import QtCore, QtGui` pattern) rather than run — rendering it needs a live Qt event loop this sandbox can't provide (see 2.1's caveat for why).

## 5. Face correlation

- [x] 5.1 Implement the nearest-position matching described in design.md decision 3: for each selected Face, compute `CenterOfMass` and find the nearest primitive by point-distance (planar) or axis-line distance (cylindrical/conical), using the position data added in task group 1; verify with a unit test (pure geometry, no FreeCAD dependency needed for the distance math itself — extract it into a plain function) covering both the planar and cylindrical/conical distance cases
- [x] 5.2 Wire the matching into the task panel: when specific Faces (not just a Body) are selected, show the per-face nearest-feature correlation section with a visible "approximate match" label, per the spec's requirement
  - **Verification caveat:** the matching logic itself (`nearest_primitive`) is verified by real, passing unit tests; `task_panel.py`'s `_build_correlation_section()` (which renders it with the required "approximate match" label) is implemented but, like 4.3, not run live in this headless sandbox.

## 6. Docs

- [x] 6.1 Add a `freecad_addon/MachinaQCAM/README.md` documenting installation (copying/symlinking into FreeCAD's `Mod/` directory), the API-server-must-be-running prerequisite, and the approximate nature of face correlation; verify by re-reading it for accuracy against the shipped addon
- [x] 6.2 Cross-link the new addon from the main `README.md` (near the `/analyze` endpoint description); verify by re-reading the updated section
